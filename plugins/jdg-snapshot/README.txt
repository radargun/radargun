Data Grid nightly build RadarGun Plug-in
--------------------------------------

This plugin is used to host the nightly Data Grid builds.

To enable this plugin rebuild RadarGun with this command and the appropriate Maven repositories:

mvn clean install -Pjdg-snapshot
